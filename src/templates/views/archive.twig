{% extends "base.twig" %}

{% block content %}
  <main class="container" id="archiveTwig">
    <h1>{{ title }}</h1>

		{% for post in posts %}
			<article>
				<h2><a href="{{ post.link }}">{{ post.title }}</a></h2>
				{{ post.content }}
			</article>
		{% endfor %}
	</main>
{% endblock %}
