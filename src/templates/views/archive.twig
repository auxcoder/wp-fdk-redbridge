{% extends "base.twig" %}

{% block content %}
  <h1>{{ title }}</h1>

  <main id="archiveTwig">
		{% for post in posts %}
			<article>
				<h1><a href="{{ post.link }}">{{ post.title }}</a></h1>
				{{ post.content }}
			</article>
		{% endfor %}
	</main>
{% endblock %}
