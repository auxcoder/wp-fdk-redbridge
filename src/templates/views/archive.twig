{% extends "base.twig" %}

{% block content %}
  <h1>{{ title }}</h1>

  <main class="container" id="archiveTwig">
		{% for post in posts %}
			<article>
				<h2><a href="{{ post.link }}">{{ post.title }}</a></h2>
				{{ post.content }}
			</article>
		{% endfor %}
	</main>
{% endblock %}
